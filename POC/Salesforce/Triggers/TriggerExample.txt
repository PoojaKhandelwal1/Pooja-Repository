public with sharing class COUTriggerHelper {
    
    // List to store trigger new list
    public static List<COU__c> COUNewList = new List<COU__c>();
    
    // List to store trigger old list
    public static List<COU__c> COUOldList = new List<COU__c>();
    
    // Map to store new Trigger Map
    public static Map<Id, COU__c> COUNewMap = new Map<Id, COU__c>();

    // Map to store Old Trigger Map
    public static Map<Id, COU__c> COUOldMap = new Map<Id, COU__c>();
	
	public static void createCOUNewMap() {
		public Map<String, Integer> COU_TypesMap = new Map<String, Integer>();
		//get total count for each type and add into map
		List<sObject> typeCountList = Database.query("select type, count(type) as COU_count from COU__c group by type");
		for(sObject obj : typeCountList) {
            if(obj.type == cou)	{
				couCount = obj.COU_count;
				
			}else if(obj.type == stock)	{
				stockCount = obj.COU_count;
			}else if(obj.type == ordo)	{
				ordoCount = obj.COU_count;
			}
       }
        for(COU__c cou : COUNewList) {
            if(//COU type= == cou)	{
				couCount++;
				cou.Id = "COU-"+couCount;
				
			}else if(//COU type= == stock)	{
				stockCount++;
				cou.Id = "STK-"+stockCount;
			}else if(//COU type= == ordo)	{
				ordoCount++;
				cou.Id = "ORD-"+ordoCount;
			}
       }
	   
}
    
	This feed item is generated by mail